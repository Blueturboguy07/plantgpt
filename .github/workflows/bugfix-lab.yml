name: bugfix-lab

on:
  workflow_dispatch:

jobs:
  oracle:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Run oracle
        shell: pwsh
        run: pwsh scripts/bugfix-lab/plantgpt-windows-macos-brew-hint-shown-on-windows.ps1
